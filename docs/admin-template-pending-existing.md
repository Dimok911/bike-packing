# Выбор существующих записей после ожидающей правки

11.09.2026. Проверка общего адаптера из `admin-template-pending-target.md`.
Публикации нет, исходные флаги OFF. Код приложения и API не менялся.

Через настоящие формы проверяются добавление существующей вещи и сумки,
замена размещённой вещи, корневой сумки и временного кармана. Выбор после
ожидающей правки данных либо параметров фиксирует отдельный неизменяемый
план, который ссылается на UUID предшественника и ждёт его подтверждения.

Матрица: 40 новых случаев — пять действий, два вида предшественника,
demo/shared и Chromium/mobile WebKit. Проверяются точные запросы, прежние
ID и поля каталога, порядок, количество и родительские связи. При замене
временного кармана его содержимое переходит в выбранную сумку. После
подтверждения новая правка использует следующую серверную версию; повторное
открытие не отправляет выполненный выбор второй раз.

Подтверждённые проверки в `node_modules/.cache/causal-evidence/2026-09-08/`:

- `resume-pending-existing-ui-1.txt`: **20/20**, 3,5 минуты. Все новые действия
  после правки данных, оба браузера без повторов/пропусков.
- `resume-pending-existing-ui-2.txt`: **40/40**, 5,3 минуты. Остальные 20
  случаев после параметров и 20 прежних проверок замены/связи. Оба браузера,
  без повторов/пропусков. Итого 40 новых +20 прежних UI-проверок.
- `resume-pending-existing-source-1.txt`: проверка исходников, exit 0.

Отказы хранения и отмена общего адаптера проверены соседними матрицами
`admin-template-pending-placement.md` и `admin-template-pending-removal.md`;
они не входят в число 40 новых случаев этого блока.

Серверный обработчик не менялся: BE `c04a6c1`, runtime `a631516`; прежний
API/MySQL445/445 относится к новым целям, заново здесь не запускался.
Локальная сборка включает отдельные изменения галереи рабочего дерева.
Личные зависимости, фотографии/архивы и общая приёмка остаются открытыми.
Сетевой отказ при reload и нативный WebKit не объявляются исправленными.
