# Ожидающий личный источник: серверный контракт

11.09.2026. Следующий блок после FE `02db586e`; BE `9ba6b37` только локально.
Публикации нет, флаги OFF.
Подключение к настоящим формам пока не выполнено: приложение всё ещё требует
подтверждённый личный источник. Этот документ не закрывает соответствующий
критерий интерфейса и весь административный раздел.

Административное сохранение с `source.kind: personal-list` теперь может
сослаться на UUID личного предшественника вместо номера версии. Клиент требует
отдельную возможность `adminTemplatePendingPersonalSourceV1` в дополнение к
прежним возможностям источника; старый сервер не получает такой запрос.
Точное тело и UUID переживают ожидание, потерю ответа и перезапуск клиента.

Сервер читает подтверждение из личного журнала для того же аккаунта и среды.
Проверяются список, вид действия, полный успешный результат и положительная
версия. Удалённый источник, отменённый/отклонённый предшественник и частично
принятый пакет не подтверждают копию. Чужой аккаунт и другой журнал с тем же
UUID не заменяют отсутствующее личное подтверждение. Ожидающее действие не
блокирует журнал до прихода предшественника.

После подтверждения предшественника сохраняются прежние проверки владельца,
приватности, namespace, удаления, текущей версии и полного SHA-256 источника.
Цель имеет собственную независимую зависимость. Проверка чтения, запись цели
и квитанция остаются одной транзакцией. Чтение квитанции не берёт блокировку
строки предшественника; порядок блокировок списков не меняется.
Повтор уже принятой копии возвращает прежний результат после удаления
личного источника и дальнейшей правки цели, не восстанавливая старые данные.

Проверки в `node_modules/.cache/causal-evidence/2026-09-08/`:

- `resume-pending-personal-client-1.txt`: **32/32**, клиент и личные планировщики.
- `resume-pending-personal-transport-1.txt`: **1285/1285**.
- `resume-pending-personal-operations-1.txt`: **158/158** серверных модульных.
- `resume-pending-personal-becheck-1.txt`: **74/74** и проверки исходников API.
- `resume-pending-personal-protocol-1.txt`: **9/9** строгого формата API.
- `resume-pending-personal-snapshot-1.txt`: **3/3** предварительной подготовки
  сохранённого личного снимка. Проверяется фиксация до вычисления хеша,
  сравнение бизнес-данных и запрет чужого контекста/ссылок на результат файлов.
  Этот чистый помощник ещё не вызывается формами.
- `resume-pending-personal-source-1.txt` и заключительный
  `resume-pending-personal-source-2.txt`: source check, exit 0.
- `resume-pending-personal-api-1.txt`: **471 passed / 3 failed**, 474 с учётом
  родительского теста, 375,36 секунды. Два дочерних отказа: ожидалось прежнее
  имя отдельной вещи, хотя существующий планировщик добавляет «копия».
  Полные данные совпали с планом. Исправлено только ожидание имени, сохранена
  точная проверка всего payload; добавлена цепочка из двух личных правок.
  Повторный полный прогон описан ниже.
- `resume-pending-personal-api-2.txt`: **475/475**, 373,58 секунды, без ошибок
  и пропусков. Включает 30 новых интеграционных случаев; серверный обработчик
  между прогонами не менялся, кроме отступов.

Далее нужны фиксация выбранной копии до переключения контекста, продолжение
личного предшественника, восстановление после reload, смена аккаунта/вкладки,
отказ хранения и реальный UI. Обратное ожидающее направление из шаблона в
личный список и файлы остаются отдельными открытыми пунктами.

При чтении кода найдено обязательное условие следующего этапа: `loadState`
выбирает снимок личного журнала, а из общего зеркала переносит только настройки
показа. Поэтому одной записи новой административной копии в общий localStorage
недостаточно для доказанного восстановления после перезагрузки. Нужно отдельно
проверить сохранение административного плана и восстановление его редактора.
Это вывод из кода; браузерный сценарий потери копии ещё не воспроизведён.
