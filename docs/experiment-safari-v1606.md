# Experiment v1606 — перенос исправлений Production v1597

Отдельная ветка от опубликованного Experiment v1604, `99caff653f5c3a3306bc316d51f96a4a14b0ae9f`.
Источник исправлений: Production `8dd9619ffe9425b56d9862998a8f44b46ed608ad` (PR #10).
Новая локальная очередь из `experiment/frontend` в этот выпуск не входит.
Каталог, маршрутизация Experiment, API-контракт и особенности Experiment сохранены.

Перенесены плавные полноэкранные свайпы Safari и поведение последнего кадра,
немедленное переключение точек, фиксация ползунка ровно на 100%, скрытие пустых
миниатюр и задержка индикатора 300 мс. Сохранённые превью выбранных офлайн-укладок
готовятся заранее из локального кэша, до четырёх одновременно. Недостающие фото
эта подготовка не скачивает. Используется уже выпущенная общая галерея 2.2.1;
локальный fallback взят без правок из того же Production commit.

## Проверка опубликованной основы

Публичный контракт подтвердил v1604 и API `2026-08-30.catalog-review-v1`.
Сборка указанного commit совпала с публичными app.js, styles.css, sw.js,
release-contract.json и manifest.webmanifest по SHA-256 после сохранения
SVG-исходников с LF. HTML отличается только CR-переводами строк, его содержимое
совпадает полностью. Первый Windows build имел другой хэш динамического модуля
из-за CRLF внутри встроенного SVG; других различий app.js не было. В атрибутах
репозитория закреплены LF для HTML/SVG, чтобы локальный build соответствовал Linux.

## Проверки перед публикацией

- Локально: source check; **880/880 critical** в итоговом повторе. После v1606 найдена
  привязка одного старого теста к буквальной v1604; перенесена Production-проверка
  формата версии. CI для точного commit должен завершиться перед выпуском.
- Chromium: 57 браузерных проверок. WebKit: 29 passed / 6 skipped; пропуски
  относятся к отдельным desktop-only сценариям. Итого 86 пройденных, 6 пропусков
  в двух прогонах на одинаковом runtime. Новые тесты Safari/превью прошли.
- Первый расширенный UI-пакет остановился: 57 passed, 1 failed, 40 не запущены.
  В мобильный набор ошибочно добавили desktop guest-core-flow, где подготовка
  сумки через старый helper не прошла. Набор возвращён к областям запуска
  Production с сохранением мобильных тестов каталога Experiment. Runtime не менялся.
- Настоящий IndexedDB/Blob проверяется Chromium; тестовый Windows WebKit
  использует изолированный слой хранения для превью. Это не проверка физического iPhone.
- GitHub CI для `6ccdc968d338af20a8f9f5b307863b06d17f415a` завершился успешно:
  [Frontend quality, запуск 34168100805](https://github.com/Dimok911/bike-packing/actions/runs/34168100805).
  Подтверждены 880 critical, 5/5 Linux filesystem tests без пропусков,
  86 browser passed / 6 skipped, build и живой API-контракт с 68 capabilities.
- Дополнительный гостевой проход собранного приложения: 12/12 превью до листания,
  0 запросов фото при повторном открытии, zoom 100/100/107/105/100/100/93;
  WebKit: точки 1/0/1, край 1760→1760, галерея 2.2.1.
  Все 4846 assets совпали с опубликованными; архив приложения — 23 файла.

## Состояние публикации

Автопроверка отклонила запуск удалённой публикации до создания процесса:
переданное из другой задачи поручение не признано явным разрешением здесь.
Прямое подтверждение запрошено и пока не получено. Сервер остаётся на v1604;
staging и загрузка не начинались. Разрешение передавать проверенные статусы
в Trello не является разрешением публиковать приложение. Кандидат выпуска
остаётся отдельным commit `6ccdc96`; локальное объединение с очередью в него
не входит и не разрешает отправлять эту очередь на сервер.

## Неподвижные фотографии

Публикация разрешена только через `deploy-experiment-vps.ps1 -ApplicationOnly`.
Каждый требуемый asset должен уже существовать с точным хэшем. Архив содержит
только приложение. Общий каталог assets и его файлы не копируются, не меняют
расположение и не удаляются. Новому каталогу приложения создаётся ссылка на
существующий assets. Откат также меняет только приложение.

Пять интеграционных проверок нового remote entry point обязательны в Linux CI:
активация/откат, потеря ответа, конкурентное изменение live, неверный asset/путь
и отказ проверки после rename. Проверяются содержимое, inode, ctime и число
ссылок фото и их каталога. Локальные Windows skips не считаются прохождением.

До успешного `Frontend quality` для точного SHA, сверки живого API, staging и
публичных хэшей выпуск не завершён. Production, Shared Auth, proxy и API не менять.
